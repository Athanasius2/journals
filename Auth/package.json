{
  "name": "auth",
  "version": "1.0.0",
  "description": "Handle authorization for Journal API. For example, make sure users cannot access other users' private journals",
  "main": "app.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.mjs"
  },
  "author": "Gabriel Kanakares",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.21.1",
    "express-session": "^1.18.1",
    "openid-client": "^5.7.0"
  }
}
